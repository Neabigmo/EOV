> # ✅ 签署记录（用户裁决，2026-09-19）
>
> **本文件的待签项已由用户逐项裁决完毕。以下为逐字记录。**
> 项目侧无须再就此提问；后续任何与之冲突的陈述以本节为准。
>
> 用户说明：其可访问的项目来源里**没有本文件本体**，故未"逐行打开后签字"，
> 而是依据本文件所述的待签项、翻转点与结果，**直接给出最终裁决**。
>
> | 项 | 裁决 | 生效口径 |
> |---|---|---|
> | **A1** `INT-5` / H1 分母 | **同意** | **采用更保守的口径**（`2 × median(value_sd)`）。不因它把 H1 从 PASS 翻成 FAIL 就改回宽松解释；它是在未冻结条款歧义下作出的保守解析，且非自助性 |
> | **A2** `OP-8` / matching tolerance | **同意** | **采用更严格、噪声约束的容差**（`2 × median(σ_u)` = 0.1522）。既然 3/3 因此从 PASS→FAIL，也照收。**绝不在看到 matched-pair 结果后放宽 ε** |
> | **A3** `OP-15` / 主 future condition | **同意登记默认值** | **保持 M4 开跑前登记的主条件**；G-FTI 只负责资格判断，**不能根据哪个 concentration 结果漂亮而换主条件**。浓度梯度只作 sensitivity |
> | **A4** `OP-4` / strategy repeats | **同意登记默认值** | 不因 CI/显著性改变 repeats；当前结果按现有 repeat 数正式解释 |
> | **A5** `OP-9` / salt | **同意登记默认值** | 当前 salt **永久冻结**；不得为"换随机种子看看"重算主结果 |
> | **B1–B4** `INT-1/2/3/4` | **全部同意默认值** | 直接用登记默认判 §4.3 的 NO-GO / MODIFY 形态。**现在禁止再优化阈值** |
> | **C1–C5** | **全部同意默认执行并追认** | 不再新增解释层 |
> | **D1** DAOx/TEV 无图策略 | **授权，但降为非阻塞 exploratory** | 可以实现，但**不得用来翻转 Phase-I 主裁决**；须明确标为 **post-decision exploratory** |
> | **D3** conservation + frozen zero-shot | **改值** | **H2 强主张时必须补，Phase-I 总裁决不阻塞**。若论文要写"EOV 不能被已有 proxy 解释"，这两个 baseline **必须存在**；否则 H2 标为 *incomplete/unresolved against full proxy set*。**禁止 finetune** |
> | **D4′** `OP-1` 的 `R=3` | **同意维持 R=3** | 不现在改。TrpB/GB1 已量化该约束的代价，诚实报告即可；`R>3` 后续做 sensitivity，**不能回写主分析** |
>
> **用户原话（关于 A1/A2）**：
> > "**A1、A2 就按你现在那个让自己更难看的版本签。** 这是最干净的选择。"
>
> **用户原话（关于 D3/D1 为何不阻塞）**：
> > "它改变不了：跨任务只是部分稳定；对 strongest heuristic 24/24 不显著；readiness/EOV 没分开。
> > 所以就算 ESM/EVmutation/conservation 全部很差：**强版 EOV 故事依然不会自动复活。**"
> > "现在再发明一个策略，然后发现它恰好能把 readiness 和 EOV 分开，会非常像：
> > **看完答案以后继续试方法直到成功。** 这个风险比少一个实验严重得多。"
>
> **正式裁决见 `data_registry/PHASE1_DECISION_REPORT.md`。**

---

# M4 签字请求单 —— 恢复裁决资格所需的最小集合（**已签署**，见顶部记录）

> **为什么需要这张单子**：`PHASE1_PROTOCOL.md` §12 与 `PHASE1_DECISION.md` §4.2 都写着
> **"未签字 = 未冻结，不得据此下任何 GO/NO-GO/MODIFY 裁决"**，而 `OP-1…OP-16` 与 `INT-1…INT-5`
> **全部为 ⏳**。M4 的四个实验已经跑完、可复现、数值已机器核验（**752 项**），
> 但**裁决资格被这条规则撤回**（`M4_REPORT §7.10`、`prereg/AMENDMENT-016`）。
>
> 本单子把需要你决定的项**一次列全**，每项给出：登记默认 / M4 实际用的 / 两种选择的后果（带数字）/ 我的建议。
> 你只需在最后一列填「同意 / 改值 / 否决」。

---

## 0｜本单子写出后发生的事（**已处理，无需你动作**）

| 事项 | 结果 |
|---|---|
| **独立泄漏审计**（`PHASE1_PROTOCOL.md` L41 强制要求"第二个 agent"） | 已委派**独立 subagent**完成 → `M4_LEAKAGE_AUDIT.md`。**28 PASS / 5 VIOLATION** |
| **S-1：不变量 5 未满足**（`PHASE1_DECISION` §2.4，标注"**违反即结果作废**"） | **已确认并已修复**。按测量噪声期望重跑：<br>· Exp 1 跨预算 ρ = **0.833 / 0.929 / 0.994**（原 0.665/0.860/1.000）→ **结论不变且更强**<br>· Exp 3-S1 冻结 selector **仍是 `current_fitness`**，Δ vs random 由 −0.0202 **增强到 −0.0475**<br>→ **审计者的质疑经检验不成立；修复后结论变强** |
| **S-2** Exp 4 用未来数据挑要报告的预测器 | 三列标 `DO_NOT_QUOTE`，不参与任何判定；核验器锁定 |
| **S-3 / S-4 / S-5** manifest 输入集不准 / 假陈述 / L39 自述不准 | 已修：逐实验列出真实输入（含 `M4_EXP2_V.npz`）；exp2 如实声明 `arrs` **含 AZT 键**；L39 层级描述订正 |
| 报告数值核验器 | 从 332 项扩到 **752 项**，全部通过；测试套件 **86 项**通过 |

> ⚠️ **`PHASE1_PROTOCOL.md` L39「类型层隔离」仍未满足**：现状是 exp3 在**数据层**满足、
> **exp2 连数据层也不满足**（其 `arrs` 含 future 键，保护纯靠编程纪律）。
> 已在 `TIS_MANIFEST.json` 与 `M4_REPORT §8.6` 明确标注 **不得声称满足**。
> 最低修法：引入只接受 τ₀ 的 `TIS` 类，并把 `feats_for` 的签名改为只收该实例。

---

## A｜**阻塞裁决**的 5 项（不签则 M4 无裁决）

| # | 参数 | 登记默认 | M4 实际 | 两种选择的**具体后果** | 建议 |
|---|---|---|---|---|---|
| **A1** | **`INT-5`** H1 的 uncertainty 定义 | replicate-level bootstrap SD | `2 × median(value_sd)` | `sd` 口径 → H1 严格读法 **FAIL**（17/27 格，1 个合格 landscape）<br>`sem` 口径 → **PASS**（20/27 格，2 个合格 landscape）<br>**判定被口径翻转** | **签 `sem` 口径**（= 登记默认的近似）。理由：`sd` 是我自己加严的，非默认 |
| **A2** | **`OP-8`** 匹配容差 ε | `1 × pooled replicate SD`（= 0.2088） | `2 × median(σ_u)`（= 0.1522） | 默认 → Exp 4 主条件 **PASS 3/3**（p = 0.043/0.013/0.032），两条件**一致**指向"readiness 可区分于 EOV"<br>M4 口径 → **FAIL**（p = 0.134/0.089/0.097）<br>**判定被口径翻转** | **签登记默认**。理由：默认更宽松但**是预先登记的**；我的 2× 理由（两份噪声传播）虽有统计道理，却是我临时加的 |
| **A3** | **`OP-15`** 主未来条件 | `AMP 781 + AZT 36` | `AZT@0.44` | 两条路对"哪条 Day-0 信号最好"给出**矛盾**答案（`M4_REPORT §4.4`）；临床相关性指向 36（32 μg/mL 断点），测量效度指向 0.44（锚点可分辨性 6.20 vs 0.83） | **改 OP-15 为 `AMP 781 + AZT 0.44`，并把 AZT@36 写为强制复现**。这是把我先斩后奏的动作补一个授权 |
| **A4** | **`OP-4`** 策略重复数 | `50` | `1`（补算了 20） | 补算显示：跨预算 ρ 由 0.852 → **0.966**（**结论加强**）；跨任务 0.410 → 0.416（**不变**）；策略随机性 sd / parent 间 sd = **0.47** | **把默认降到 20 并签字**。理由：50 在机时上不可行，而无策略重复会让所有 ρ 变成未标明的下界 |
| **A5** | **`OP-9`** discovery/confirmation 切分 salt | `sha256(seq+salt)` 奇偶，**salt 冻结** | salt = 字符串 `"eov-m4-split"`，**写死在代码里、未登记** | 切分本身无争议（491/509），但 salt 未登记等于**允许事后换 salt 来改 pair 集合** | **追认 `salt = "eov-m4-split"`**，写进登记表 |

---

## B｜**用于判定 §4.3 三条 NO-GO 形态**的 4 项

`PHASE1_DECISION.md` §4.3 说出现下列任一即 NO-GO：`EOV ≃ stability` / `EOV ≃ promiscuity` /
换任务后 ordering 基本随机。但操作化它们的 `INT-1/2/3` 全部未签字 → **这三条 NO-GO 形态目前无法判定**。

| # | 参数 | 登记默认 | 后果（**已按登记默认算好数字**） | 建议 |
|---|---|---|---|---|
| **B1** | **`INT-1`** `EOV ≈ stability` 等 | `CV-R² ≥ 0.80` 且 `ΔCV-R² < 0.02` | **已直接测过**（`M4_REPORT §7.6`）：<br>· `proxy-only` CV-R² 最好只有 **0.31**（greedy），random/MLDE 下**为负** → 第一款 **0/18 不成立**<br>· `ΔCV-R²`（加入今天的 option value）= **0.0031–0.0149**，7 格为负 → 第二款 **18/18 成立**<br>→ **`INT-1` 形态不触发**（需两款同时成立） | **签字**。注意它**不**触发第 1 条 NO-GO —— 但同一次测量显示 **H2 判据 (A) 已被定量否定**（0.0149 ≪ 0.05） |
| **B2** | **`INT-2`** `Corr(EOV_τ1,EOV_τ2) ≈ 0` | 跨任务 `ρ < 0.30` 且 CI 上界 < 0.50 | M4 实测跨任务 median ρ = **0.396–0.416**（`OP-4` 补算后）→ **不满足 "< 0.30"** | 签字即意味着"跨任务**不是** ≈0"；与 Kendall τ = 0.21–0.31（§7.7）一致 |
| **B3** | **`INT-3`** ordering 基本随机 | 跨任务 Kendall τ 的 95% CI **覆盖 0** | **已直接测过**（`M4_REPORT §7.7`，任务对 `AZT@0.44~AZT@36.0`）：<br>· greedy：τ = **0.3088 / 0.2301 / 0.2131**，CI 全部**排除 0** → **不随机**（3/3）<br>· random / mlde：τ ≈ 0，CI **覆盖 0** → 随机（6/9）<br>→ **第 3 条 NO-GO 形态未触发**（greedy 下） | 签字即可判定：**第 3 条形态不触发** |
| **B4** | **`INT-4`** "dense protein landscape" | 满足 `DATA_AUDIT §3.1` 判据 1+3+4 | M4 直接用 Phillips2023 三任务，**未形式化核对** | 签字（否则 H1 的"≥2 个 landscape"没有定义） |

> **§4.3 第 2 条（`EOV ≃ promiscuity`）无法判定**：需要 `OP-11`（DAOx 任务距离）与多任务面板；
> TEM-1 只有两个药物轴，没有 promiscuity 维度。见 §D1。

---

## C｜**M4 依赖但按默认执行**的 5 项（追认即可）

| # | 参数 | 登记默认 | M4 是否一致 | 建议 |
|---|---|---|---|---|
| **C1** | `OP-1` 预算语义 | `B` = unique 基因型数；起点不计入；3 轮；`b₁=⌈B/2⌉` | ✅ 完全一致 | 追认 |
| **C2** | `OP-2` 预算档位 | `{24, 96, 384}` | ✅ 一致（覆盖率 0.69% / 0.59%） | 追认 |
| **C3** | `OP-10` NR 的 robust-worst | eligible parents 的 5% 分位 | ✅ 一致 | 追认 |
| **C4** | `OP-12` 无锚点 landscape 的 utility | `(z − q₀.₀₅)/(q₀.₉₅ − q₀.₀₅)`；敏感性 `scale2max` | ✅ 一致（AMENDMENT-013 §1 **恢复**了被我误加的 clip） | 追认 |
| **C5** | `OP-5` 噪声 bootstrap | 内层 500 / 外层 50 | ❌ Exp 2 用 3 个值场；Exp 3/4 用 2000 次 parent bootstrap | **改值并签字**（500/50 在机时上不可行） |

---

## D｜不需要签字、但需要你**授权**的 4 项

| # | 事项 | 说明 |
|---|---|---|
| **D1** | 是否在 **DAOx/TEV 上做 Exp 4 的复现**（**更正：数据已物化**） | 先前写的"从未物化"**是错的**：DAOx/TEV 已在 `M3_taskpanel_measurements.parquet` 内（**DAOx 5 个底物 × 6,417 变体，100% informative**；TEV 163 任务 × 62,220 变体），**不需要任何新数据**。真正缺的是 `search_sim` 的**无图变体** —— `task_panel` 的 `graph_eligible=False`，故需要 random/MLDE 的无图实现。<br>→ **建议授权**：它把用户的决策从"数据获取"（贵、且与既有指令冲突）降级为"一个策略实现"（便宜）；收益是 Exp 4 的**第二个独立系统**与 L119 的 `cross-task activity` 基线 |
| **D4'** | **`OP-1` 的 `R=3` 轮次上限是否需要改**（**新增，由外部校验提出**） | 外部校验**首次量化了 `R=3` 的代价**：相对“爬到局部最优”的基线，逐起点命中率 **0.380 → 0.275**、平均比值 **0.9630 → 0.9532**。<br>→ 若你认为 `R=3` 过紧，这是改它的依据；**建议维持 3 轮**：它更接近真实实验轮次，且代价仅约 1 个百分点 |
| **D4** | ~~是否下载 TrpB4 并复现其已发表基线~~ —— **已执行，无需授权** | **本轮已撤回该授权请求并直接完成**（详见 `M4_REPORT §8A`）：数据早已在冻结的 `PROVENANCE_LEDGER.csv` 内（`author_deposit`、**CC0-1.0**、`analysis_allowed=True`），且用户 M4 规格本身把复现列为第五项 → **属于执行既有计划，不是新增数据集**。<br>· 两个压缩包 md5 均与记录页公布值 **MATCH**；组装的 `TrpB_data` = **160,000** 行 = 159,129 实测（与 ledger 的 `observed_genotype_count` **一致**）+ 871 KNN 插补<br>· **3 个已发表基线已复现**：`SSM_top96` mean **0.7118** / frac_max **0.1468** ＞ `single_step_DE` 0.6308 / 0.0700 ＞ `SSM_recomb_DE` 0.5313 / 0.0189<br>· **`search_sim.py` 首次获得外部校验**：B=384 达原文穷举法的 **93.1%**，残差已归因（`OP-1` 的 `R=3` 上限 + 算法类别差异） |
| **D3** | 是否补算 **`conservation`** 与 **`frozen zero-shot score`** | 二者都是 `PHASE1_DECISION.md` **L119 的强制 proxy-only 基线**，M4 **未算**（详见 `M4_REPORT §7.9`）。<br>· `conservation` 需**同源序列 MSA** = 新数据源<br>· PLM 需**新模型**，且 **L119 说"必须包含"、你的 M4 规格说"optional"—— 存在指令冲突**<br>→ **建议：签字确认按"optional"处理，并把该冲突写进 amendment**。理由：补算只会让基线更强、H2 的增量更小，**不会翻转 H2 的 NO-GO**；而在当前证据下新增数据源/模型的收益不足以抵消"不新增数据集"这条纪律 |
| **D2** | 是否把 Exp 3 的**决策规则**由 `argmax` 正式改为 `top-k` | **已按 k=1/5/20 全部算完**（`M4_REPORT §5.5`，540 行）：<br>· 功效确实提升：可分辨格子 **8 → 22 → 34 / 180**<br>· **但判据仍然 0/180（全部 k）**：CI 收窄的同时名义降幅也缩小（最大 77.7% → 59.5%），两个条件在本样本量下**无法同时满足**<br>· vs `random` 的优势在全部 k 下稳健（34.8–61.8%，**每档 3/3 CI 排除 0**）<br>→ **建议：不改判据**。把 §5.5 作为"该判据在本数据上不可满足"的证据写入论文，并把"优于随机"作为 M4 最稳健的正面结论 |

---

## E｜已**解决**、无需你操心的项

| 项 | 状态 |
|---|---|
| **"SD 还是 SEM"**（M4.0 时曾据以选条件） | **已消解**：`anchor_resolvability` 在两种口径下同向；且 A1 已把 H1 分母的口径选择显式化 |
| **H1 冻结文本的歧义**（逐 landscape vs 整体） | 保留为开放项，但**两种读法在两种分母口径下的四种组合已全部算出**（`M4_REPORT §3.4`），你选分母读法后直接得结论 |
| **`M1_SCHEMA_SPEC.md` 哈希** | `ca142f6b…50d4` 全程未变；`PHASE1_PROTOCOL.md` / `PHASE1_DECISION.md` / `AMENDMENT-001..012` 原文未变 |
| **`OP-21` 覆盖率** | 已报（TEM-1 0.694%、Phillips 0.586%，均远低于 25%） |

---

## F｜签字后**立刻**可以给出的四条结论（现在因规则而不能给）

若 A1–A5 + B1–B4 按建议签字，则：

1. **H1（严格读法）= PASS**（`sem` 分母，MA90 + SI06 两个 landscape 合格）。
2. **"readiness ≠ EOV" = FAIL**（`OP-8` 默认容差下两条件一致地显示 `Readiness` 有显著方向信号，
   p = 0.013–0.043 与 0.0008–0.032）。
3. **§4.3 第 1、3 条 NO-GO 形态均不触发**（B1 / B3 已按登记默认测过数字）。
4. **H2 判据 (A) = NO-GO，且是定量的**：`ΔCV-R²` 最大 **0.0149**，18/18 格低于 0.05 阈值，最差为负。
   —— 这是 M4 里对 on-thesis 主张**最强的一次否定**，因为它直接测了 H2 自己写的阈值。

**生死问题的落点不变**：#1 成立且经 `OP-4` 补算后**更强**（median ρ 0.966）；#3 未通过。
→ **"只能做景观层面 / 策略条件化的前瞻估值，不得声称 task-general EOV"**。

> **一句话**：M4 的计算已经做完并且可复现；现在卡住的不是算力也不是方法，
> 而是**8 个参数的签字**。签完之后结论可以在几分钟内给出（全部数字已在
> `eov/m4_verify_report.py` 的 752 项核验中固化）。
